# <f:cObject typoscriptObjectPath="lib.tx_hisodat.search.resultnumbers" data="{counter.cycle}" />
# currently only working for steps with 10

lib.tx_hisodat.search.resultnumbers = COA
lib.tx_hisodat.search.resultnumbers {

	5 = LOAD_REGISTER
	5 {
	
		currentPage.cObject.cObject = TEXT
		currentPage.cObject.cObject {
			data = GP : tx_hisodat_search|__widget_0|currentPage
			wrap = |-1
		}
		currentPage.cObject = TEXT
		currentPage.cObject {
			prioriCalc = intval
			dataWrap = |{field:counter}
		}
	
	}
	
	10 = TEXT
	10 {
		data = register : currentPage
		if.isPositive.data = register : currentPage
		
		override.cObject = TEXT
		override.cObject {
			if.value = 10
			if.equals.field = counter
			data = GP : tx_hisodat_search|__widget_0|currentPage
			wrap = |0
		}
	}
	
	20 = TEXT
	20 {
		field = counter
		if.isPositive.data = register : currentPage
		if.negate = 1
	}

}